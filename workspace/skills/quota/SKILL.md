---
name: quota
description: Check remaining Gemini API requests for today. Use when the user types /quota or asks how many API requests are left.
---

# Quota Skill

When the user invokes /quota or asks about remaining API requests, run this command:

```
python3 /home/pranav/gemini_counter.py status
```

Then reply with the output exactly as returned. Nothing else needed.
